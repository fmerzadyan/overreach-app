import React from 'react';
import WorldMap from './WorldMap';

const HomePage = () => {
    return (
        <div className="container">
            <WorldMap />
        </div>
    );
};

export default HomePage;
